# 护理推送垂直切片（应用代码）

医护电脑 Web + 患者微信扫码 H5。

## 本地

```bash
npm install
npm run dev
```

打开 <http://localhost:3000/app/login>。口令见 `.env.example`。恢复种子：`npm run db:reset`。

对外测试与 Vercel / Neon 配置见仓库根目录 [README.md](../README.md)。只发系统管理员 `admin`，由对方在「账号权限」页建号。
